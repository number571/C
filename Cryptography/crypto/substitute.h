extern char set_alpha_substitute (char * const vector);
extern char set_vector_substitute (char * const vector);
extern char substitute (
    char * const to, 
    const char mode, 
    char * const from
);